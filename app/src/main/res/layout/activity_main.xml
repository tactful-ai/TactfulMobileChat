<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="30sp"
            android:layout_marginBottom="30sp"
            android:fontFamily="sans-serif-black"
            android:text="Tactful Mobile Chat Testing"
            android:textAlignment="center"
            android:textColor="#0C646E"
            android:textSize="24sp" />
        <!-- URL input -->
        <EditText
            android:id="@+id/inputUrl"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter widget URL (e.g. https://example.com/embed)"
            android:inputType="textUri"
            android:imeOptions="actionDone"
            android:singleLine="true" />

        <!-- Clear Button -->
        <Button
            android:id="@+id/btnClearUrl"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:backgroundTint="#0C646E"
            android:text="Clear" />

        <!-- Current URL display -->
        <TextView
            android:id="@+id/txtCurrentUrl"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Current URL: (not set)"
            android:textAppearance="?android:textAppearanceMedium" />


    </LinearLayout>

    <!-- Bubble (FAB) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabWidget"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:contentDescription="@string/open_widget"
        app:backgroundTint="#0C646E"
        app:srcCompat="@drawable/ic_chat_bubble"
        app:tint="@android:color/white" />
</FrameLayout>
